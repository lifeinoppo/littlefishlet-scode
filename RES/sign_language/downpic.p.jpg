#!/usr/bin/python
import urllib

def downpic(addr,name):
	urllib.urlretrieve(addr,name)

file = open('../pic_addr_2').readlines()
file2 = open('../pic_name_2').readlines()
for i in range(len(file)):
	downpic(file[i],file2[i])
	print file2[i]+'ok!'

